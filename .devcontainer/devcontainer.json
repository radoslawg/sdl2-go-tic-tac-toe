{
  "name": "SDL2 in GoLang",
  "dockerFile": "Dockerfile",

  "features": {
    "ghcr.io/meaningful-ooo/devcontainer-features/homebrew:2": []
    //         "ghcr.io/devcontainers-extra/features/neovim-homebrew:1": []
  },

  "postCreateCommand": "brew install neovim fzf zoxide ripgrep fd && cd /root/.config/nvim && git pull && chown -R root:root /workspaces && git config --global --add safe.directory /workspaces/sdl2-go",
  "postAttachCommand": "cd /root/.config/nvim && git pull"
  //    "remoteUser": "root",
  //    "containerUser": "root",
}
